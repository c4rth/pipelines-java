variables:
- group: VariableGroup
- name: global_variable
  value: value_global-overwrite_1 
- name: directory_include
  value: ./include
- template: ${{ variables.directory_include }}/variables.yml

trigger:
  batch: true
  branches:
    include:
    - master

jobs:

- job: jobA   
  steps:
  - template: simple-param.yml
  displayName: "Simple param - default"

- job: jobB
  dependsOn: [jobA]
  displayName: "Simple param"
  steps:
  - template: simple-param.yml
    parameters:
      yesNo: true
  - template: simple-param.yml
    parameters:
      yesNo: false